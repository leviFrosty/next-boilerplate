pre-commit:
  parallel: true
  jobs:
    - run: bunx pretty-quick --staged
      stage_fixed: true

    - run: bunx eslint --fix "{staged_files}"
      glob:
        - '*.ts'
        - '*.js'
        - '*.tsx'
        - '*.jsx'
      stage_fixed: true
