pre-commit:
  parallel: true
  jobs:
    - run: pnpm exec pretty-quick --staged
      stage_fixed: true

    - run: pnpm exec eslint --fix "{staged_files}"
      glob:
        - '*.ts'
        - '*.js'
        - '*.tsx'
        - '*.jsx'
      stage_fixed: true
