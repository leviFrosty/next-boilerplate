pre-commit:
  parallel: true
  jobs:
    - run: pnpm exec pretty-quick --staged
      stage_fixed: true

    - run: pnpm turbo lint
      glob:
        - '*.ts'
        - '*.js'
        - '*.tsx'
        - '*.jsx'
      stage_fixed: true
